mixin videos(video)
    a(href=`/video/${video.id}`) 
        div.thumb__container 
            img.thumb__img(src= isFly ? video.thumbUrl : "/" + video.thumbUrl, alt="thumbnail")
        div.video__text-data
            h4.video__title=video.title
            p.video__owner=video.owner.name
            p.video__views 조회수 #{video.meta.views} 회