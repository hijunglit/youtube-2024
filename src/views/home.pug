extends base
include ../mixins/video

block content
    ul.video__grid
        each video in videos
            li.video__item 
                +videos(video)
        else 
            p sorry nothing found.
block scripts 
    script(src="/static/js/main.js") 